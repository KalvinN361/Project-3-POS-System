const require bcrypt from('becrypt');

/*
 * grab user input and pass to bcrypt
 * bcrypt will handle pw hashing, then pw will be stored in db 
 */

// let submit = document.getElementsById('btn')
// // submit.addEvent {

// bcrpt.hash(password, hash, function(err, result) {

// }) 
// bcrypt.campare(notPassword, hash, function(err, result){

// })